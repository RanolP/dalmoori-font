title: 달무리 글꼴 - Dalmoori
email: public.ranolp@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  달무리는 현대 한글 전체를 지원하는 것을 목표로 개발중인 8*8 한글 글꼴입니다.
baseurl: "/dalmoori-font" # the subpath of your site, e.g. /blog
url: "https://ranolp.github.io" # the base hostname & protocol for your site, e.g. http://example.com
github_username: RanolP

# Build settings
plugins:
  - jekyll-feed

include: ["_pages"]

permalink: /:categories/:title/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
