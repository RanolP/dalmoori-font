import { css } from '@emotion/core';

const Style = css`
  html {
    font-size: 16px;
  }
  html, select {
    font-family: dalmoori;
  }
`;

export const KeepAll = css`
  html {
    word-break: keep-all;
  }
`;

export default Style;
